#include <stdio.h>

int main(void)
{
  int foo, bar, baz;
  float foobar, foobaz, barbaz;

  printf("%d and %d went down to the %d\n", foo, bar, baz);
  printf("but %f already %f the %f\n", foobar, foobaz, barbaz);

  return 0;
}
